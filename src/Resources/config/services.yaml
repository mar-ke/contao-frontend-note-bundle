# src/Resources/config/services.yaml
services:
  _defaults:
    autowire: true
    autoconfigure: true

  Mar-ke\ContaoFrontendPostitBundle\EventListener\GeneratePageListener:
    tags:
      - { name: 'contao.hook', hook: 'generatePage' }